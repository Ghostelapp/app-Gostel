/**
 * DEPRECATED — CallKeep bridge binding has been moved into IncomingCallProvider
 * so we don't open a duplicate WebSocket connection. This file is kept only as
 * a no-op stub to avoid breaking any stray imports.
 */
export default function CallKeepBridge(): null {
  return null;
}
